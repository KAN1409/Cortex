package com.kareem.cortex;

/** Type of knowledge derived from a signal; separate from signal disposition. */
public enum CognitiveKind {
    ACTION,
    WAITING,
    DECISION,
    EVENT,
    CONTENT,
    MESSAGE,
    REMINDER,
    INSIGHT,
    MEMORY
}
