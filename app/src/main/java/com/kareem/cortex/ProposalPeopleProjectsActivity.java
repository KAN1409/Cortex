package com.kareem.cortex;

import android.content.Intent;
import android.graphics.Color;
import android.text.*;
import android.view.*;
import android.widget.*;
import java.util.*;

/** PRIME People / Projects with locked matte cards, monoline icons and per-result proposals. */
public final class ProposalPeopleProjectsActivity extends PeopleProjectsActivity {
    @Override void build(){
        LinearLayout root=new LinearLayout(this);root.setOrientation(LinearLayout.VERTICAL);root.setBackgroundColor(CortexUi.BG);LinearLayout body=new LinearLayout(this);body.setOrientation(LinearLayout.VERTICAL);body.setPadding(dp(18),dp(10),dp(18),0);root.addView(body,new LinearLayout.LayoutParams(-1,0,1));body.addView(header());
        tabs=new LinearLayout(this);tabs.setOrientation(LinearLayout.HORIZONTAL);tabs.setPadding(0,dp(13),0,dp(8));addTab("People","people");addTab("Projects","projects");body.addView(tabs,new LinearLayout.LayoutParams(-1,dp(59)));
        LinearLayout sb=CortexUi.card(this,18);sb.setOrientation(LinearLayout.HORIZONTAL);sb.setGravity(Gravity.CENTER_VERTICAL);sb.setPadding(dp(9),dp(4),dp(10),dp(4));sb.addView(CortexUi.glyph(this,"search",CortexUi.GREEN,false),new LinearLayout.LayoutParams(dp(38),dp(38)));search=new EditText(this);search.setHint("Search people or projects");search.setHintTextColor(CortexUi.FAINT);search.setTextColor(CortexUi.TEXT);search.setTextSize(13);search.setSingleLine(true);search.setBackgroundColor(Color.TRANSPARENT);search.setPadding(dp(9),0,0,0);sb.addView(search,new LinearLayout.LayoutParams(0,dp(44),1));LinearLayout.LayoutParams sp=new LinearLayout.LayoutParams(-1,dp(52));sp.setMargins(0,0,0,dp(7));body.addView(sb,sp);search.addTextChangedListener(new TextWatcher(){public void beforeTextChanged(CharSequence s,int st,int c,int a){}public void onTextChanged(CharSequence s,int st,int before,int count){render(lastRows,lastMode);}public void afterTextChanged(Editable e){}});
        ScrollView sv=new ScrollView(this);sv.setFillViewport(true);sv.setClipToPadding(false);feed=new LinearLayout(this);feed.setOrientation(LinearLayout.VERTICAL);feed.setPadding(0,dp(5),0,dp(24));sv.addView(feed);body.addView(sv,new LinearLayout.LayoutParams(-1,0,1));CortexUi.addBottomNav(this,root,"people",null);setContentView(root);styleTabs();
    }

    View header(){LinearLayout row=new LinearLayout(this);row.setGravity(Gravity.CENTER_VERTICAL);row.setPadding(dp(5),dp(8),dp(2),dp(10));View dot=new View(this);dot.setBackground(CortexUi.round(this,CortexUi.GREEN,Color.TRANSPARENT,999));row.addView(dot,new LinearLayout.LayoutParams(dp(9),dp(9)));TextView c=CortexUi.plain(this,"C O R T E X",14,CortexUi.TEXT);CortexUi.bold(c);if(android.os.Build.VERSION.SDK_INT>=21)c.setLetterSpacing(.20f);LinearLayout.LayoutParams cp=new LinearLayout.LayoutParams(-2,dp(40));cp.setMargins(dp(12),0,0,0);row.addView(c,cp);View d=CortexUi.divider(this);LinearLayout.LayoutParams dv=new LinearLayout.LayoutParams(dp(1),dp(28));dv.setMargins(dp(12),0,dp(12),0);row.addView(d,dv);TextView sys=CortexUi.plain(this,"PEOPLE / PROJECTS",10,CortexUi.MUTED);if(android.os.Build.VERSION.SDK_INT>=21)sys.setLetterSpacing(.07f);row.addView(sys,new LinearLayout.LayoutParams(0,dp(40),1));CortexGlyphView archive=CortexUi.glyph(this,"file",CortexUi.GREEN,false);archive.setOnClickListener(v->startActivity(new Intent(this,VaultActivity.class)));row.addView(archive,new LinearLayout.LayoutParams(dp(46),dp(46)));return row;}

    @Override void styleTabs(){if(tabs==null)return;for(int i=0;i<tabs.getChildCount();i++){TextView t=(TextView)tabs.getChildAt(i);boolean on=mode.equals(t.getTag());int color="people".equals(t.getTag())?CortexUi.GREEN:CortexUi.YELLOW;t.setTextColor(on?CortexUi.TEXT:CortexUi.MUTED);int wash=on?Color.argb(18,Color.red(color),Color.green(color),Color.blue(color)):Color.TRANSPARENT;t.setBackground(CortexUi.round(this,wash,on?Color.argb(80,Color.red(color),Color.green(color),Color.blue(color)):CortexUi.BORDER_SOFT,999));CortexUi.raised(this,t,on?3:1);if(on)CortexUi.medium(t);}}

    @Override void addRow(Row r){
        int color=r.candidate?CortexUi.ORANGE:CortexUi.GREEN;String icon="people".equals(mode)?"person":"project";
        LinearLayout card=CortexUi.card(this,18);card.setPadding(0,0,0,0);LinearLayout main=new LinearLayout(this);main.setGravity(Gravity.CENTER_VERTICAL);main.setPadding(dp(11),dp(11),dp(10),dp(10));
        View rail=new View(this);rail.setBackground(CortexUi.round(this,color,Color.TRANSPARENT,999));LinearLayout.LayoutParams rp=new LinearLayout.LayoutParams(dp(2),dp(46));rp.setMargins(0,0,dp(10),0);main.addView(rail,rp);main.addView(CortexUi.glyph(this,icon,color,true),new LinearLayout.LayoutParams(dp(46),dp(46)));
        LinearLayout tx=new LinearLayout(this);tx.setOrientation(LinearLayout.VERTICAL);LinearLayout.LayoutParams xp=new LinearLayout.LayoutParams(0,-2,1);xp.setMargins(dp(11),0,dp(8),0);main.addView(tx,xp);TextView n=CortexUi.text(this,r.name,15,CortexUi.TEXT);CortexUi.medium(n);n.setMaxLines(2);tx.addView(n);String meta=r.candidate?"Project candidate · needs confirmation":(r.identified?"Identified person":"Confirmed project")+(r.mentions>0?"  •  "+r.mentions+" grounded reference"+(r.mentions==1?"":"s"):"");TextView m=CortexUi.plain(this,meta,10,r.candidate?CortexUi.ORANGE:CortexUi.MUTED);m.setPadding(0,dp(4),0,0);tx.addView(m);main.addView(CortexUi.chip(this,r.candidate?"REVIEW":"CONTEXT",color,true),new LinearLayout.LayoutParams(-2,dp(32)));card.addView(main);main.setOnClickListener(v->detail(r));

        ArrayList<ContextRef> recent=r.candidate?new ArrayList<>():safeRecent(r);StringBuilder result=new StringBuilder();result.append(r.candidate?"Project candidate":"people".equals(mode)?"Identified person":"Confirmed project").append(": ").append(r.name);if(r.mentions>0)result.append("\nGrounded references: ").append(r.mentions);for(int i=0;i<Math.min(2,recent.size());i++){ContextRef x=recent.get(i);result.append("\nContext: ").append(x.title==null?"":x.title);if(x.preview!=null&&!x.preview.trim().isEmpty())result.append(" — ").append(proposalClip(x.preview,260));}long sourceId=recent.isEmpty()?0:recent.get(0).itemId;
        LinearLayout host=new LinearLayout(this);host.setOrientation(LinearLayout.VERTICAL);host.setPadding(dp(12),0,dp(10),dp(8));card.addView(host);ProposalUi.attach(this,db,host,new ResultProposalEngine.Target("People / Projects","entity_"+r.kind+"_"+r.id,r.name,result.toString(),sourceId,r.kind,false));LinearLayout.LayoutParams cp=new LinearLayout.LayoutParams(-1,-2);if(feed.getChildCount()>0)cp.setMargins(0,dp(8),0,0);feed.addView(card,cp);
    }

    @Override void openEntityBrain(Row r,long focal){String subject=r.identified?"identified person":"confirmed project";CortexActionExecutor.openBrain(this,focal,"Focus on the "+subject+" ‘"+r.name+"’. Use grounded Cortex evidence connected to this exact "+(r.identified?"person":"project")+", including the attached latest evidence when present. Tell me the useful current context, open loops, recent changes, decisions, and the best next actions. Do not infer facts from the name alone.");}
    private ArrayList<ContextRef> safeRecent(Row r){try{return recentContext(r);}catch(Throwable e){return new ArrayList<>();}}
    private static String proposalClip(String s,int n){String x=s==null?"":s.replaceAll("\\s+"," ").trim();return x.length()>n?x.substring(0,n)+"…":x;}
}
