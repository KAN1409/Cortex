# Continue Cortex master fix

Read MASTER_FIX_STATUS.md first. This branch contains a tested foundation, not a completed redesign or full intelligence system. Preserve it and implement remaining work. User authorization includes code fixes, testing and compatible installation on the connected phone; never uninstall, clear storage, change signing/package, or merge to main without instruction.

Target: organized Life and Work data, understandable at a glance, with source-backed findings and direct user control. Required outcome: capture -> normalize -> evidence -> candidate -> judge -> publish -> truthful action -> feedback. Avoid assistant-first layouts, empty slogans, and treating installed models as evidence that reasoning works.

Priorities:
1. Review current shared publisher and run ownership against runtime tests. Manual and Environment triggers must use the unique worker. Validate actual UI work status and recovery on the phone.
2. Complete evidence/claim quality checks and typed adapters across notifications, screenshots, work data, voice, documents and existing semantic events. Preserve originals and ambiguous identities. Do not populate NOW by relaxing quality gates.
3. Consolidate remaining publication/attention paths, reasoning tiers, and scheduling. Tier 0 deterministic comparisons first, small models where available, heavy council manual unless explicitly enabled by the user. Preserve source browsing independent of NOW limits.
4. Implement truthful action states, local read-back verification, idempotence, and external handoff states. Opening an external app must not count as completion.
5. Implement bounded feedback-based ranking and explicit Life/Work organization. Maintain persistent dismissal and correction history.
6. Test an isolated golden scenario: comparable marble PO 1000 EGP/m2, new quotation 1180 EGP/m2, explicit vendor confirmation pending and next-day deadline. Verify calculated 18% increase, source-specific support, correct deadline, no duplication, UI visibility, every source opening, feedback, action verification, and restart persistence. Do not seed fake findings into normal user data.
7. Test malformed identifiers, incompatible currencies/units/scope, missing evidence, simultaneous runs, Activity destruction and process death. A failed model with an honest error proves failure handling only.
8. Install a compatible tested update over the existing app, then demonstrate one real evidence-to-result flow. Report remaining failures honestly. Do not declare completion based only on builds or component inventory.

Known device: RKGL102BGDH; package com.kareem.cortex. Last verified installed build: 158. This branch builds 159. No v159 phone verification yet. Device access requires the user to unlock it; never request their PIN. Final local APK/test report paths are documented in MASTER_FIX_STATUS.md. Local tests: gradle :app:testDebugUnitTest --tests com.kareem.cortex.CortexInsightPublisherTest --tests com.kareem.cortex.CouncilExecutionLeaseTest --tests com.kareem.cortex.DiscoveryV3PolicyTest :app:assembleDebug.

Communicate briefly in Egyptian Arabic. Preserve a precise checkpoint when quota is restrictive. No private phone database, model files, API keys, or raw diagnostic exports should be committed.
