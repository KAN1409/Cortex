android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Lifecycle/Compose lint checks are compiled against a newer Kotlin analysis API than AGP 8.7.3's bundled lint.
# Android's supported fallback for projects that cannot upgrade AGP yet is to override the lint engine.
android.experimental.lint.version=8.8.2
