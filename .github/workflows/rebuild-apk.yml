name: Cortex Rebuild APK

on:
  push:
    branches:
      - rebuild/cortex-v1
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Verify Cortex 0.7.2 GPT-OSS brain policy
        shell: bash
        run: |
          set -euo pipefail
          grep -q "applicationId 'com.kareem.cortex.rebuild'" app/build.gradle
          grep -q "versionCode 9" app/build.gradle
          grep -q "versionName '0.7.2-gptoss-brain'" app/build.gradle
          grep -q 'openai/gpt-oss-120b' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'reasoning_effort' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'json_schema' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'strict.*true' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'Gemini fallback' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'CortexBrainRouter.understand' app/src/main/java/com/kareem/cortex/rebuild/CaptureAnalysisQueue.java
          grep -q 'CortexBrainRouter.understand' app/src/main/java/com/kareem/cortex/rebuild/BrainIntakeQueue.java
          grep -q 'TEST_META' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'PRODUCT_FEEDBACK' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'TRANSIENT' app/src/main/java/com/kareem/cortex/rebuild/CortexBrainRouter.java
          grep -q 'CREATE TABLE IF NOT EXISTS brain_evidence_policy' app/src/main/java/com/kareem/cortex/rebuild/BrainStore.java
          grep -q 'CREATE TABLE IF NOT EXISTS product_feedback' app/src/main/java/com/kareem/cortex/rebuild/BrainStore.java
          grep -q '72L\*60L\*60L\*1000L' app/src/main/java/com/kareem/cortex/rebuild/BrainStore.java
          grep -q 'purgeExpiredShortEvidence' app/src/main/java/com/kareem/cortex/rebuild/CortexApp.java
          ! grep -q 'RECENT VOICE CAPTURES' app/src/main/java/com/kareem/cortex/rebuild/MainActivity.java
          ! grep -q 'recentVoiceOutcomes' app/src/main/java/com/kareem/cortex/rebuild/MainActivity.java
          grep -q 'signer_generated=0' scripts/prepare-cortex-rebuild-signer-recovery.sh
          grep -q 'signer_replaced=0' scripts/prepare-cortex-rebuild-signer-recovery.sh
          ! grep -q 'openssl genpkey' scripts/prepare-cortex-rebuild-signer-recovery.sh
          ! grep -q 'keytool -genkey' scripts/prepare-cortex-rebuild-signer-recovery.sh

      - name: Build debug and unsigned release APKs
        run: gradle :app:assembleDebug :app:assembleRelease --stacktrace

      - name: Verify release artifact is unsigned
        shell: bash
        run: |
          set -euo pipefail
          APK="app/build/outputs/apk/release/app-release-unsigned.apk"
          test -s "$APK"
          APKSIGNER="$(find "$ANDROID_HOME/build-tools" -type f -name apksigner | sort -V | tail -n1)"
          test -n "$APKSIGNER"
          if "$APKSIGNER" verify "$APK" >/dev/null 2>&1; then
            echo "Release artifact unexpectedly carries a signer" >&2
            exit 1
          fi

      - name: Package non-destructive signer diagnostic
        shell: bash
        run: |
          set -euo pipefail
          OUT="dist/Cortex-Rebuild-Signer-Recovery"
          mkdir -p "$OUT"
          cp scripts/prepare-cortex-rebuild-signer-recovery.sh "$OUT/prepare-cortex-rebuild-signer-recovery.sh"
          chmod +x "$OUT/prepare-cortex-rebuild-signer-recovery.sh"
          cat > "$OUT/README.txt" <<'EOF'
          Cortex Rebuild signer continuity diagnostic

          This bundle is intentionally NON-DESTRUCTIVE. It snapshots the installed
          com.kareem.cortex.rebuild app data and compares the installed APK signer against the
          authoritative existing local Cortex signer at ~/Cortex/app/cortex-debug.keystore.

          It NEVER generates, replaces, reconstructs or rotates a signing key.
          It NEVER uninstalls, clears, installs or replaces Cortex.
          A signer mismatch is reported as a hard update-in-place block.
          EOF
          (cd "$OUT" && sha256sum prepare-cortex-rebuild-signer-recovery.sh > SHA256SUMS.txt)

      - name: Upload Cortex 0.7.2 diagnostic debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Cortex-0.7.2-GPTOSS-Brain-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload signer recovery diagnostic
        uses: actions/upload-artifact@v4
        with:
          name: Cortex-Rebuild-Signer-Recovery
          path: dist/Cortex-Rebuild-Signer-Recovery/
          if-no-files-found: error
