name: Build Cortex APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-debug-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Cortex
        uses: actions/checkout@v6

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v6
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle :app:assembleDebug --stacktrace

      - name: Prepare APK artifact
        shell: bash
        run: |
          mkdir -p dist
          cp app/build/outputs/apk/debug/app-debug.apk dist/Cortex-v1.0.0-debug.apk
          sha256sum dist/Cortex-v1.0.0-debug.apk > dist/Cortex-v1.0.0-debug.sha256

      - name: Upload Cortex APK
        uses: actions/upload-artifact@v4
        with:
          name: Cortex-v1.0.0-debug
          path: dist/
          if-no-files-found: error
          retention-days: 30
